name: security

on:
  push:
  pull_request:
  schedule:
    - cron: "23 6 * * 1"

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  bandit:
    name: Bandit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Bandit
        run: |
          python -m pip install -U pip setuptools wheel
          python -m pip install bandit
      - name: Full Bandit report
        run: |
          mkdir -p security-artifacts
          bandit -r pramagent deploy/dashboard -f json -o security-artifacts/bandit.json || true
          bandit -r pramagent deploy/dashboard -f txt -o security-artifacts/bandit.txt || true
      - name: Gate on Bandit findings
        run: bandit -r pramagent deploy/dashboard
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bandit-security-report
          path: security-artifacts/bandit.*

  semgrep:
    name: Semgrep
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Full Semgrep report
        run: |
          mkdir -p security-artifacts
          docker run --rm -v "$PWD:/src" -w /src semgrep/semgrep:latest \
            semgrep scan --metrics=off --config p/security-audit --config p/python \
            --json --output security-artifacts/semgrep.json pramagent deploy/dashboard || true
          docker run --rm -v "$PWD:/src" -w /src semgrep/semgrep:latest \
            semgrep scan --metrics=off --config p/security-audit --config p/python \
            --text --output security-artifacts/semgrep.txt pramagent deploy/dashboard || true
      - name: Gate on Semgrep findings
        run: |
          docker run --rm -v "$PWD:/src" -w /src semgrep/semgrep:latest \
            semgrep scan --metrics=off --config p/security-audit --config p/python \
            --error pramagent deploy/dashboard
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: semgrep-security-report
          path: security-artifacts/semgrep.*

  pip-audit:
    name: Dependency vulnerability scan (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package + supply-chain tooling
        run: |
          python -m pip install -U pip setuptools wheel
          python -m pip install -c requirements-security.txt ".[all,dev]"
      - name: Full pip-audit report
        run: |
          mkdir -p security-artifacts
          python -m pip_audit -f json -o security-artifacts/pip-audit.json || true
          python -m pip_audit -f columns -o security-artifacts/pip-audit.txt || true
          python -m cyclonedx_py environment -o security-artifacts/sbom.cdx.json
      - name: Gate on known vulnerabilities
        run: python -m pip_audit
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pip-audit-report
          path: |
            security-artifacts/pip-audit.*
            security-artifacts/sbom.cdx.json

  zap:
    name: OWASP ZAP Authenticated OpenAPI Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install API sidecar
        run: |
          python -m pip install -U pip setuptools wheel
          python -m pip install -e ".[api]"
      - name: Start sidecar and run ZAP OpenAPI report
        env:
          PRAMAGENT_API_KEY: ci-test-key
          PRAMAGENT_API_KEYS: default:ci-test-key
          PRAMAGENT_JWT_SECRET: pramagent-ci-jwt-secret-v0-7-zap-6b43f1e98bd04dc6
          PRAMAGENT_SIGNING_KEY: pramagent-ci-signing-key-v0-7-zap-91e4dbf7c2504bb8
          PRAMAGENT_ALLOW_MEMORY_STORE: "1"
        run: |
          mkdir -p security-artifacts
          python -m uvicorn pramagent.api.app:app --host 0.0.0.0 --port 8080 &
          api_pid=$!
          trap "kill $api_pid || true" EXIT
          for i in {1..30}; do
            curl -fsS http://127.0.0.1:8080/health && break
            sleep 2
          done
          curl -fsS http://127.0.0.1:8080/health
          chmod 777 security-artifacts
          zap_auth="-config replacer.full_list(0).description=auth-header -config replacer.full_list(0).enabled=true -config replacer.full_list(0).matchtype=REQ_HEADER -config replacer.full_list(0).matchstr=Authorization -config replacer.full_list(0).replacement=Bearer\ ci-test-key"
          docker run --rm --user root --network host -v "$PWD/security-artifacts:/zap/wrk/:rw" \
            ghcr.io/zaproxy/zaproxy:stable zap-api-scan.py \
            -t http://127.0.0.1:8080/openapi.json -f openapi \
            -J zap_api_authenticated.json -r zap_api_authenticated.html -w zap_api_authenticated.md \
            -z "$zap_auth" -I
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-security-report
          path: security-artifacts/zap_api_authenticated.*
